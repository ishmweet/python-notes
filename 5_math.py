x = 3.14
y = 4
z = 5

#result = round(x)      # round gives the nearest round off number
#result = abs(y)        # abs is the absolute value (distance from zero)
#result = pow(2, 3)     # pow calculates a raised to the power of b or simply 2^3 in this example
#result = max(x, y, z)  # displays the max value
result = min(x, y, z)  # displays the min value

print(result)